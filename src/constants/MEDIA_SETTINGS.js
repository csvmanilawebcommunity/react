// NOTES:
//  - This is not set to const so that we can alter the value with a mute button
//  - Recommend setting this on every now and then (or always) during development.
//    Unlcean code which is not caught by eslint causes the background music to not play.
//    If this happens, there's a problem with your changes.
export let BGM_VOLUME = 1; // 0 or 1 only
export let CRY_VOLUME = 1; // 0 or 1 only
export let VOICE_VOLUME = 1; // 0 or 1 only